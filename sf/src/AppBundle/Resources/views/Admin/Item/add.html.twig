{% extends 'AppBundle::layout.html.twig' %}

{% block title %} Admin/Item! {% endblock %}

{% block body %}

    <h1> Add action for items Admin/Item/add.html.twig </h1>
    <hr>

    {{ form_start(form) }}
    {{ form_widget(form) }} {#print form#}
    <button>Save</button>
    {{ form_end(form) }}

    {#{{ dump(item) }}#}

{% endblock %}